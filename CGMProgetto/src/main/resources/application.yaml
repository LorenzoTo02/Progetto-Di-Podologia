server:
      port: 9026
      error:
            path: /error
            whitelabel:
                  enabled: false

spring:
      datasource:
                driver-class-name: com.mysql.cj.jdbc.Driver
                url: jdbc:mysql://localhost:3306/lezionidb
                username: Oliver
                password: oliver
      jpa:
          hibernate:
                    dialect: org.hibernate.dialect.MySQL8Dialect
                    ddl-auto: update
                    show_sql: true
                
